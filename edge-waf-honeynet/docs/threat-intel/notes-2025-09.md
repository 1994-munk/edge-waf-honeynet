# Threat-intel notes — Sept 2025

- Saw a noticeable spike in `xmlrpc.php` POSTs from a single /24 in
  AS-XXXX. Added a temporary block in `local-overrides.conf` (id 1200)
  with a 7-day expiry comment.
- Cowrie captured a new credential list — added to the bait userdb so
  future runs of the same list trip the alert faster.
- Reminder: rotate the bait DB password in `honeypot/http/honey.conf`
  every quarter so leaks are easy to date.
